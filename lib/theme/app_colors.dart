import 'package:flutter/material.dart';

class AppColors {
  // === BACKGROUNDS ===
  static const bgDark = Color(0xFF05060A);
  static const bg = Color(0xFF0C0D12);
  static const bgLight = Color(0xFF14151B);
  static const cardBackground = Color(0xFF0C0D12);
  static const inputFill = Color(0xFF14151B);

  // === TEXT ===
  static const textButton = Color(0xFF0C0D12);
  static const text = Color(0xFFE9E8E8);
  static const textMuted = Color(0xFFB5B8C2);
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFB5B8C2);
  static const onPrimary = Color(0xFF05060A);

  // === ACCENTS ===
  static const primary = Color(0xFF9AA8FF);
  static const secondary = Color(0xFFD6C26A);
  static const highlight = Color(0xFF6E717A);

  // === BORDERS ===
  static const border = Color(0xFF585B63);
  static const borderMuted = Color(0xFF40434A);

  // === STATUS ===
  static const danger = Color(0xFFC98787);
  static const warning = Color(0xFFC9C087);
  static const success = Color(0xFF87C9A8);
  static const info = Color(0xFF87AFC9);

  // === INPUTS ===
  static const inputHint = Color(0xFFB5B8C2);
}
